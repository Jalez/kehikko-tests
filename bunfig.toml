# A DOM for every test file, registered once.
#
# The rendering tests need one and the pure ones do not care, and a preload is
# cheaper to reason about than two suites with different globals — the failure
# where a component test runs without a document is a stack trace nobody reads as
# "wrong config".
[test]
preload = ["./test/setup.ts"]
